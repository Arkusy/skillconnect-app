-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.app_legal_docs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  doc_type text NOT NULL UNIQUE,
  title text NOT NULL,
  content text NOT NULL,
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT app_legal_docs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.categories (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL UNIQUE,
  icon character varying NOT NULL,
  description text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.conversations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  participant_1 uuid NOT NULL,
  participant_2 uuid NOT NULL,
  last_message_at timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  last_message_preview text,
  url text,
  is_help_channel boolean DEFAULT false,
  CONSTRAINT conversations_pkey PRIMARY KEY (id),
  CONSTRAINT conversations_participant_1_fkey FOREIGN KEY (participant_1) REFERENCES public.profiles(id),
  CONSTRAINT conversations_participant_2_fkey FOREIGN KEY (participant_2) REFERENCES public.profiles(id)
);
CREATE TABLE public.issues (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  reporter_id uuid NOT NULL,
  order_id bigint,
  type character varying NOT NULL,
  priority character varying DEFAULT 'medium'::character varying,
  status character varying DEFAULT 'open'::character varying,
  description text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  resolved_at timestamp with time zone,
  reported_user_id uuid,
  CONSTRAINT issues_pkey PRIMARY KEY (id),
  CONSTRAINT issues_reported_user_id_fkey FOREIGN KEY (reported_user_id) REFERENCES public.profiles(id),
  CONSTRAINT issues_reporter_id_fkey FOREIGN KEY (reporter_id) REFERENCES public.profiles(id),
  CONSTRAINT issues_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.messages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  conversation_id uuid NOT NULL,
  sender_id uuid NOT NULL,
  receiver_id uuid NOT NULL,
  message_text text CHECK (char_length(TRIM(BOTH FROM message_text)) > 0),
  is_read boolean DEFAULT false,
  read_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  media_url text,
  message_type text DEFAULT 'text'::text,
  CONSTRAINT messages_pkey PRIMARY KEY (id),
  CONSTRAINT messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.conversations(id),
  CONSTRAINT messages_sender_id_fkey FOREIGN KEY (sender_id) REFERENCES public.profiles(id),
  CONSTRAINT messages_receiver_id_fkey FOREIGN KEY (receiver_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  type character varying NOT NULL,
  title text NOT NULL,
  message text NOT NULL,
  data jsonb DEFAULT '{}'::jsonb,
  is_read boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.orders (
  id bigint NOT NULL DEFAULT nextval('orders_id_seq'::regclass),
  user_id uuid NOT NULL,
  worker_id uuid NOT NULL,
  category_id uuid NOT NULL,
  customer_name text NOT NULL,
  customer_phone text NOT NULL,
  customer_address text NOT NULL,
  problem_description text,
  problem_image_url text,
  initiate_otp text,
  complete_otp text,
  status character varying DEFAULT 'pending'::character varying CHECK (status::text = ANY (ARRAY['pending'::character varying, 'accepted'::character varying, 'initiated'::character varying, 'completed'::character varying, 'cancelled'::character varying, 'rejected'::character varying]::text[])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  accepted_at timestamp with time zone,
  initiated_at timestamp with time zone,
  completed_at timestamp with time zone,
  cancelled_at timestamp with time zone,
  pricing_type character varying,
  price_amount numeric,
  currency character varying,
  hours_worked numeric,
  extra_parts_cost numeric DEFAULT 0,
  total_price numeric,
  is_rated boolean DEFAULT false,
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT orders_worker_id_fkey FOREIGN KEY (worker_id) REFERENCES auth.users(id),
  CONSTRAINT orders_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  full_name text NOT NULL,
  email text NOT NULL,
  role integer NOT NULL DEFAULT 2,
  avatar_url text,
  bio text,
  phone text,
  address text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_profile_complete boolean DEFAULT ((full_name IS NOT NULL) AND (full_name <> ''::text) AND (phone IS NOT NULL) AND (phone <> ''::text) AND (address IS NOT NULL) AND (address <> ''::text)),
  expo_push_token text,
  latitude double precision,
  longitude double precision,
  is_verified boolean DEFAULT false,
  verification_tier text DEFAULT 'none'::text,
  one_star_count integer DEFAULT 0,
  last_warning_at timestamp with time zone,
  demotion_count integer DEFAULT 0,
  last_demotion_at timestamp with time zone,
  last_demotion_reason text,
  is_banned boolean DEFAULT false,
  ban_expires_at timestamp with time zone,
  ban_reason text,
  banned_by uuid,
  banned_at timestamp with time zone,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id),
  CONSTRAINT profiles_banned_by_fkey FOREIGN KEY (banned_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.ratings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  worker_profile_id uuid NOT NULL,
  user_id uuid NOT NULL,
  rating integer NOT NULL CHECK (rating >= 1 AND rating <= 5),
  review text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  order_id bigint UNIQUE,
  CONSTRAINT ratings_pkey PRIMARY KEY (id),
  CONSTRAINT ratings_worker_profile_id_fkey FOREIGN KEY (worker_profile_id) REFERENCES public.worker_profiles(id),
  CONSTRAINT ratings_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT ratings_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.worker_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  category_id uuid NOT NULL,
  experience_years integer DEFAULT 0,
  availability_status character varying DEFAULT 'available'::character varying,
  service_description text,
  skills ARRAY,
  certifications ARRAY,
  average_rating numeric DEFAULT 0.00,
  total_ratings integer DEFAULT 0,
  total_jobs_completed integer DEFAULT 0,
  is_verified boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  pricing_type character varying DEFAULT 'hourly'::character varying CHECK (pricing_type::text = ANY (ARRAY['hourly'::character varying, 'fix'::character varying]::text[])),
  price numeric,
  currency character varying DEFAULT 'USD'::character varying CHECK (currency::text = ANY (ARRAY['INR'::character varying, 'USD'::character varying]::text[])),
  portfolio_images ARRAY,
  CONSTRAINT worker_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT worker_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT worker_profiles_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id)
);
CREATE TABLE public.worker_status_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  action text NOT NULL,
  reason text,
  previous_role integer,
  new_role integer,
  performed_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT worker_status_history_pkey PRIMARY KEY (id),
  CONSTRAINT worker_status_history_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT worker_status_history_performed_by_fkey FOREIGN KEY (performed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.worker_subscriptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  plan_type text NOT NULL,
  started_at timestamp with time zone NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  previous_month_earnings numeric DEFAULT 0,
  base_fee numeric DEFAULT 99,
  percentage_fee numeric DEFAULT 0,
  total_fee numeric DEFAULT 0,
  fee_currency text DEFAULT 'INR'::text,
  is_reupgrade boolean DEFAULT false,
  is_paid boolean DEFAULT false,
  payment_reference text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  is_free_trial_used boolean DEFAULT false,
  CONSTRAINT worker_subscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT worker_subscriptions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.worker_verification (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  aadhar_image_url text NOT NULL,
  selfie_image_url text NOT NULL,
  alternate_phone text,
  service_areas ARRAY,
  certifications text,
  status text DEFAULT 'pending'::text,
  rejection_reason text,
  reviewed_by uuid,
  reviewed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  utr_number text,
  payment_amount numeric,
  payment_submitted_at timestamp without time zone,
  CONSTRAINT worker_verification_pkey PRIMARY KEY (id),
  CONSTRAINT worker_verification_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT worker_verification_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.profiles(id)
);